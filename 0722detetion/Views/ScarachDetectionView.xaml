<UserControl
    x:Class="_0722detetion.Views.ScarachDetectionView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:_0722detetion.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:halcon="clr-namespace:HalconDotNet;assembly=HalconDotNet"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition  />
            <ColumnDefinition  />
            
        </Grid.ColumnDefinitions>

        <Grid.RowDefinitions>
            <RowDefinition Height="auto"/>
            <RowDefinition/>
            <RowDefinition Height="auto"/>
        </Grid.RowDefinitions>

        <TextBlock Text="检测图片" HorizontalAlignment="Center" FontSize="20" Grid.Column="0" Grid.Row="0"/>
        <TextBlock Text="检测结果" HorizontalAlignment="Center" FontSize="20" Grid.Column="1" Grid.Row="0"/>
        <Border BorderBrush="Gray" BorderThickness="1" Grid.Column="0" Grid.Row="1" Margin="1">
            <halcon:HSmartWindowControlWPF/>
        </Border>
        <Border BorderBrush="Gray" BorderThickness="1" Grid.Column="1" Grid.Row="1" Margin="1">
            <halcon:HSmartWindowControlWPF/>
        </Border>
        <Button Content="开始检测" Grid.Row="2" Grid.ColumnSpan="2" Width="100"/>
    </Grid>
</UserControl>
